<UserControl x:Class="DnsRouteUI.Views.ResolverProfilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolVis" />
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <!-- 左侧列表 -->
        <DockPanel Grid.Column="0" Margin="12">
            <TextBlock DockPanel.Dock="Top" Text="上游解析配置档" Style="{StaticResource SectionHeader}" FontSize="16" />
            <TextBlock DockPanel.Dock="Top" Text="规格第 5.2 节：配置档代表一套可共享的 DNS 解析上下文。" Foreground="#666" TextWrapping="Wrap" Margin="0,0,0,8" />
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                <Button Content="新建" Command="{Binding NewCommand}" />
                <Button Content="编辑" Command="{Binding EditCommand}" />
                <Button Content="删除" Command="{Binding DeleteCommand}" />
                <Button Content="测试连通性" Command="{Binding TestConnectivityCommand}" />
            </StackPanel>

            <ListBox ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfile}" Margin="0,8,0,0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="2">
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" />
                            <TextBlock Text="{Binding Forwarders, StringFormat=转发器: {0}}" Foreground="#666" FontSize="11"
                                       TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>

        <!-- 右侧编辑区 -->
        <Border Grid.Column="1" Margin="12" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4"
                Visibility="{Binding IsEditing, Converter={StaticResource BoolVis}}">
            <ScrollViewer Padding="16">
                <StackPanel>
                    <TextBlock Text="编辑配置档" Style="{StaticResource SectionHeader}" FontSize="16" />

                    <TextBlock Text="配置档名称" Foreground="#555" />
                    <TextBox Text="{Binding SelectedProfile.Name, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Text="转发器列表（一行一个 IPv4 地址）" Foreground="#555" Margin="0,8,0,0" />
                    <TextBox Text="{Binding ForwardersText, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True" Height="100" VerticalScrollBarVisibility="Auto"
                             FontFamily="Consolas" />

                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <CheckBox Content="启用递归" IsChecked="{Binding SelectedProfile.EnableRecursion}" VerticalAlignment="Center" Margin="0,0,16,0" />
                        <CheckBox Content="启用缓存隔离" IsChecked="{Binding SelectedProfile.CacheIsolationEnabled}" VerticalAlignment="Center" />
                    </StackPanel>

                    <TextBlock Text="缓存范围名称（自动生成，允许高级修改）" Foreground="#555" Margin="0,8,0,0" />
                    <TextBox Text="{Binding SelectedProfile.CacheScopeName, UpdateSourceTrigger=PropertyChanged}" FontFamily="Consolas" />

                    <TextBlock Text="备注" Foreground="#555" Margin="0,8,0,0" />
                    <TextBox Text="{Binding SelectedProfile.Note, UpdateSourceTrigger=PropertyChanged}" Height="60" AcceptsReturn="True" />

                    <TextBlock Text="{Binding ReferenceCount}" Foreground="#4C8DCE" Margin="0,8,0,0" />

                    <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                        <Button Content="保存" Command="{Binding SaveCommand}" />
                        <Button Content="取消" Command="{Binding CancelCommand}" />
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- 未编辑提示 -->
        <TextBlock Grid.Column="1" Text="选择左侧配置档并点击“编辑”，或点击“新建”创建配置档。"
                   Foreground="#999" FontSize="13" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsEditing}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </Grid>
</UserControl>
